[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    integration: end-to-end tests that touch the FastAPI TestClient
    slow: tests that take more than a couple seconds
